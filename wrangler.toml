name = "az-movies"
main = "router-v8.js"
compatibility_date = "2026-09-07"
keep_vars = true
# TMDB en worker.js. Servidores y formatos vienen del bridge que ya devuelve servidores_disponibles + streams.
# Solo TMDB_API_KEY es obligatoria aquí.

[secrets]
required = ["TMDB_API_KEY"]
